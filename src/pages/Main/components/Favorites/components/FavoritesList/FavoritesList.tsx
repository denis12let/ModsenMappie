import { FC, ReactNode } from 'react';
import { FavoriteItem } from '../FavoriteItem';

export const FavoritesList: FC = () => {
  return (
    <>
      <FavoriteItem />
      <FavoriteItem />
    </>
  );
};
